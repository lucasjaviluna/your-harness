{
  "name": "your-harness",
  "version": "0.1.0",
  "private": true,
  "description": "Operating System for Software Engineering.",
  "license": "MIT",

  "packageManager": "pnpm@10",

  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=10.0.0"
  },

  "type": "module",

  "scripts": {
    "build": "turbo run build",
    "clean": "turbo run clean",
    "dev": "turbo run dev",
    "check": "turbo run check"
  },

  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "^5.9.2"
  }
}
